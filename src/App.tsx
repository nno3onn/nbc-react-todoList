import "./App.css";
import Layout from "./shared/Layout";
import Router from "./shared/Router";

const App: React.FC = () => {
  return (
    <Layout>
      <Router />
    </Layout>
  );
};

export default App;
